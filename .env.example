# LLM Provider Configuration
# Choose one provider: either Anthropic API or AWS Bedrock

# ============ Option 1: Anthropic API (Default) ============
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here

# ============ Option 2: AWS Bedrock ============
# Configure these if using AWS Bedrock instead of Anthropic API
# Note: You can also configure these in the app Settings UI

# AWS Region where Bedrock is available
# AWS_REGION=us-east-1

# Option A: Use explicit credentials
# AWS_ACCESS_KEY_ID=your_access_key_id
# AWS_SECRET_ACCESS_KEY=your_secret_access_key
# AWS_SESSION_TOKEN=your_session_token (optional, for temporary credentials)

# Option B: Use a named profile from ~/.aws/credentials
# AWS_PROFILE=default

# Development mode
NODE_ENV=development
